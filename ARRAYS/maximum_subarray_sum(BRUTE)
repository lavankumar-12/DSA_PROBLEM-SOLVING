int maximum_subarray_sum(vector<int>& a)
{
    int n=a.size();
    int max_sum=INT_MIN;
    for(int i=0;i<n;i++)
    {
        int sum=0;
        for(int j=i;j<n;j++)
        {
            sum+=a[j];
            max_sum=max(max_sum,sum);
        }
    }
    return max_sum;
}