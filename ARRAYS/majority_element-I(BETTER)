int majority_element_n_2(vector<int>& a)
{
    map<int,int> mpp;
    for(int i=0;i<a.size();i++)
    {
        mpp[a[i]]++;
    }
    for(auto it:mpp)
    {
        if(it.second>(a.size()/2))
        {
            return it.first;
        }
    }
    return -1;
}