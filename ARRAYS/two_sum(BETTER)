int two_sum(vector<int>& a,int target)
{
    map<int,int> mpp;
    for(int i=0;i<a.size();i++)
    {
        if(mpp.find(target-a[i])!=mpp.end())
        {
            return true;
        }
        mpp[a[i]]++;
    }
    return false;
}