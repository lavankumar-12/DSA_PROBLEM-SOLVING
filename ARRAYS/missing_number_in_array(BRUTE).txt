int missing(vector<int>& a)
{
  for(int i=0;i<a.size();i++)
  {
     int flag=0;
     for(int j=0;<a.size();j++)
     {
       if(a[j]==i)
	{
           flag=1;
	   break;
	}
     }
     if(flag==0) return i;
  }
return -1;
}